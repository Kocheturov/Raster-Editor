class TextBox:
    """
    Класс представляет текстовый блок с настраиваемым текстом, 
    позицией, форматированием и возможностью трансформации.
    """

    def __init__(self, text, position):
        """
        Инициализирует экземпляр TextBox.

        Аргументы:
            text (str): Текст для текстового блока.
            position (tuple): Позиция текстового блока (x, y).
        """
        self.text = text
        self.position = position
        self.font_family = "Arial"
        self.font_size = 12
        self.color = (0, 0, 0)
        # ... другие параметры форматирования

    def set_font_family(self, font_family):
        """
        Устанавливает семейство шрифтов для текстового блока.

        Аргументы:
            font_family (str): Название семейства шрифтов.
        """
        # Установка семейства шрифтов
        self.font_family = font_family

    def set_font_size(self, font_size):
        """
        Устанавливает размер шрифта для текстового блока.

        Аргументы:
            font_size (int): Размер шрифта.
        """
        # Установка размера шрифта
        self.font_size = font_size

    # ... другие методы для форматирования текста

    def transform(self, type, **kwargs):
        """
        Трансформирует текстовый блок (поворот, масштабирование).

        Аргументы:
            type (str): Тип трансформации (например, "rotate", "scale").
            **kwargs: Дополнительные аргументы для трансформации.
        """
        # Трансформация текстового блока (поворот, масштабирование)
        # ...
