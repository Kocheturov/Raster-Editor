class Effect:
    """
    Класс представляет эффект, который можно применить к изображению, 
    с настраиваемым типом.
    """

    def __init__(self, type):
        """
        Инициализирует экземпляр Effect.

        Аргументы:
            type (str): Тип эффекта (например, "shadow", "glow").
        """
        self.type = type
        # ...

    def apply_effect(self, image):
        """
        Применяет эффект к изображению в соответствии с типом эффекта.

        Аргументы:
            image (Image): Изображение, к которому нужно применить эффект.
        """
        # Применение эффекта к изображению
        if self.type == "shadow":
            # ... добавить тень к изображению
        elif self.type == "glow":
            # ... добавить свечение к изображению
        # ...
