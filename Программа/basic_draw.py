class DrawingTool:
    """
    Представляет инструмент рисования с настраиваемыми свойствами, 
    такими как тип, размер, цвет и форма. Обрабатывает события 
    ввода пользователя для выполнения действий рисования.
    """

    def __init__(self, type):
        """
        Инициализирует экземпляр DrawingTool.

        Аргументы:
            type (str): Тип инструмента рисования (например, "brush", "pencil").
        """
        self.type = type
        # ... другие параметры, например, размер, цвет, форма

    def activate(self):
        """
        Активирует инструмент рисования, делая его готовым к вводу 
        пользователя.
        """
        # Активация инструмента
        # ...

    def deactivate(self):
        """
        Деактивирует инструмент рисования, прекращая его реакцию на 
        ввод пользователя.
        """
        # Деактивация инструмента
        # ...

    def handle_input(self, event):
        """
        Обрабатывает события ввода пользователя (например, щелчки мыши, 
        перемещения) и выполняет действия рисования в зависимости от 
        типа инструмента.

        Аргументы:
            event (Event): Событие ввода пользователя для обработки.
        """
        if self.type == "brush":
            # ... рисование кистью
        elif self.type == "pencil":
            # ... рисование карандашом
        # ...
